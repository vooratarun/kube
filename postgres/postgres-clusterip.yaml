apiVersion: v1
kind: Service
metadata:
  name: postgres-clusterip
  namespace: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres   # MUST match pod labels
  ports:
    - port: 5432
      targetPort: 5432
